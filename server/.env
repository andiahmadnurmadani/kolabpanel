DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=kolabpanel
PORT=5000
JWT_SECRET=supersecretkey
STORAGE_ROOT=../userdata

# Where user projects are stored. Supports:
# - Local drive: D:/KolabPanel (recommended for speed)
# - Mapped drive: X:/kolabpanel (works, slower over SMB/Tailscale)
# - UNC path: \\100.90.80.70\web\kolabpanel (recommended over mapped drive)

# Temp folder for uploaded zip/files (keep on local SSD for speed)
# UPLOAD_TEMP=E:/Workspace/kolabpanel/server/uploads

# Path for payment proof uploads (organized by username)
PAYMENT_PROOF_PATH=../user_billing

# phpMyAdmin URL (adjust based on your installation)
# XAMPP default: http://localhost/phpmyadmin
# Custom port: http://localhost:8080/phpmyadmin
PHPMYADMIN_URL=http://localhost/phpmyadmin

# Email Configuration (Gmail SMTP)
SMTP_USER=rahmanfaisal653@gmail.com
SMTP_PASSWORD=qlau yzux baow ddai
APP_URL=http://localhost:3000

# SSH Configuration for Laravel deployment on Synology
# Use Tailscale IP to bypass Cloudflare proxy
SSH_HOST=100.90.80.70
SSH_PORT=22
SSH_USER=Aslabkolab
SSH_PASSWORD=ASLAB@kolablogin123
# Or use SSH key (recommended):
# SSH_PRIVATE_KEY_PATH=/path/to/private_key


# Publish strategy when deploying to STORAGE_ROOT:
# - swap: try rename swap (fast), fallback to copy if rename fails
# - copy: always copy into final folder (recommended for some SMB shares that block rename)
DEPLOY_PUBLISH_MODE=swap
